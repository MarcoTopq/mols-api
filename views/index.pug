extends layout

block content
  h1= title
  p Welcome to #{title}
  button(onclick="location.href='/download'" type='button')
    | download r18
  button(onclick="location.href='/download'" type='button')
    | download non-r18
  

  